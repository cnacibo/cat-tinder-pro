name: cat_tinder_pro
description: "Tinder app for cats."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.4.1
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^10.0.0
  provider: ^6.1.5+1
  get_it: ^9.2.1
  firebase_analytics: ^12.1.3
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  assets:
    - .env